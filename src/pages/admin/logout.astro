---
export const prerender = false;

Astro.cookies.set('admin', '', {
	path: '/',
	maxAge: 0,
	httpOnly: true,
	sameSite: 'lax'
});

return new Response(null, {
	status: 302,
	headers: {
		'Location': '/admin/login',
		'Cache-Control': 'no-cache'
	}
});
---
